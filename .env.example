KAKAO_REST_API_KEY=
KAKAO_JAVASCRIPT_KEY=
WEATHER_API_KEY=
# Disabled by default. Never copy or use the host's shared OPENAI_API_KEY.
# Enable only after explicitly authorizing a separate project key.
WHERE_FOOD_ENABLE_OPENAI=0
WHERE_FOOD_OPENAI_API_KEY=
WHERE_FOOD_OPENAI_MODEL=gpt-5-mini
FLASK_SECRET_KEY=
# Optional: defaults to instance/where_food.db in the project directory.
# DATABASE_PATH=instance/where_food.db
# Set to 1 when serving through HTTPS.
COOKIE_SECURE=0
